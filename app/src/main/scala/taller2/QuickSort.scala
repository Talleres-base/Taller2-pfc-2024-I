package taller2;

class QuickSort {

  type AlgoritmoOrd[T] = List[T] => (List[T], Int)
  type Comparador[T] = (T, T) => Boolean

  def menoresQue_noMenoresQue[T](l:List[T], v:T, comp:Comparador[T])
}
